企业服务呼叫中心后台管理模板：
	1、首页为企业信息管理列表
	2、列表内有复选框、查看、编辑、审核、报备
	3、列表头部有新增（跳转到新页面）、后退、前进、刷新、按条件查询
	4、分页只有左右箭头到下/上一页
	
	5、左边栏目导航为树形结构图 
	6、顶部有密码修改、注销、帮助
	
晶科物流管理系统后台模板
	1、登录页面没有点击输入密码功能
	2、开票页面有日期选择框、下拉框、复选框、单选框等组合
	3、清单页面有删除、编辑、添加线路、打印
	4、上框有帮助、注销、开票员的工作平台、发送短息
	
	

简单通用后台管理框架模板
	1、登录页面为黑色半透明输入框、记住账号、登录
	2、主页面有个当前用户管理（个人信息、切换账户、退出）、更换页面颜色模式
	3、点击新功能，会创建新的标签页面

蓝色精美简洁MUI后台管理系统
	1、登录后显示用户头像、前台首页、清除缓存、退出登录
	2、语言切换中、英
	3、内容管理表格有添加内容、关键字搜索、复选框、修改、删除、批量删除、上页下页尾页功能。
	
	
	
	$('#dg').datagrid('getPager').pagination({
	    displayMsg:'当前显示从{from}到{to}共{total}记录',
	    onBeforeRefresh:function(pageNumber, pageSize){
	     $(this).pagination('loading');
	     alert('pageNumber:'+pageNumber+',pageSize:'+pageSize);
	     $(this).pagination('loaded');
	    },
	   });